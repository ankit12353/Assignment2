<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <include
        android:id="@+id/include_toolbar"
        layout="@layout/toolbar"/>
    <androidx.recyclerview.widget.RecyclerView
        android:layout_below="@id/include_toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/rv_student_list"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tv_display_noRecords"
        android:layout_centerInParent="true"
        android:textSize="@dimen/text_normal"
        android:textColor="@color/colorGreen"
        android:text="@string/label_no_records_yet"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btn_addStudent"
        android:background="@drawable/common_border"
        android:text="@string/label_btn_add_student"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>